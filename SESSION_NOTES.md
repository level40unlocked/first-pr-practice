# 2D 캐릭터 애니메이션 자동화 — 세션 기록

## 목표
AI로 매 장면을 새로 그리지 않고, **캐릭터 그림을 한 번만 준비하면 그 이후 모든 움직임(입/눈/고개/팔다리/색/텍스처)은 코드로 무료 재사용**하는 파이프라인을 만드는 것. 장기 연재 시리즈에서 캐릭터 일관성 유지 + 생성 비용 절감이 목적.

---

## 1. 리포에 커밋된 핵심 스크립트

| 파일 | 역할 |
|---|---|
| `animate_image.py` | 정지 이미지에 팬/줌(Ken Burns) 효과 |
| `lipsync_video.py` | 오디오 볼륨에 맞춰 입 모양(닫힘/중간/열림) 스왑 |
| `pose_video.py` | 미리 그린 포즈 몇 장을 순환시켜 "움직이는 것처럼" 보이게 함 |
| `body_wiggle.py` | 이미지 전체를 하나의 강체로 살짝 튕기고 기울이는 애니메이션 (자르지 않음) |
| `talking_head_video.py` | 몸은 고정, 머리만 원형 레이어로 독립적으로 까딱이며 입/눈 스왑 |

테스트: `python -m unittest discover -p "test_*.py"` (44개 테스트 통과 확인됨)

---

## 2. 핵심 기술적 발견

### 2.1 원(circle)은 회전해도 이음새가 없다
머리를 원형으로 크롭하면 어떤 각도로 회전시켜도 실루엣이 똑같아서, 몸통과의 경계에 seam이 생기지 않음. `talking_head_video.py`의 핵심 원리.

### 2.2 AnimatedDrawings (Meta, 오픈소스) — 진짜 관절 리깅
- ARAP 메시 변형 기반. `texture.png` + `mask.png` + `char_cfg.yaml`(관절 좌표)로 구동.
- **A-포즈 필수**: 팔다리가 몸통 실루엣과 겹치면(주머니에 손 넣기 등) 메시 삼각형이 꼬여서 렌더링 시 몸통이 찌그러짐. 팔은 몸통에서 확실히 떨어뜨려 그려야 함.
- 헤드리스 컨테이너에서 `xvfb-run`으로 OpenGL 렌더링 가능.

### 2.3 "목 없는" 원본이 핵심
- **최선**: 머리 전용 이미지 + 몸통 전용 이미지(목이 아예 없는 상태)를 처음부터 따로 생성 → 나중에 이어붙임. 패치가 전혀 필요 없음.
- **차선**: 이미 합쳐진 그림 한 장만 있을 때는 mediapipe로 턱선을 추적해서 원형 마스크를 만들고, 남은 목 피부는 옷깃 색으로 리터치. 손이 훨씬 많이 감.
- 기존 캐릭터를 유지해야 하면 한 장으로(패치 필요), 새로 설계할 수 있으면 머리/몸통 분리 생성(패치 불필요) — 상황에 따라 선택.

### 2.4 Plan A vs Plan B (몸 전체 리깅 + 얼굴)
- **Plan A (권장)**: 몸은 정적 이미지, 머리만 독립 레이어로 까딱 + 입/눈 스왑. 해상도 원본 그대로라 화질 좋고 안정적.
- **Plan B (대안)**: 합쳐진 이미지 통째로 AnimatedDrawings에 넣고, 머리 위치를 매 프레임 이미지 분석(구멍 감지)으로 추적해서 입/눈을 직접 그려 넣음. 별도 머리 에셋 불필요하지만 해상도 낮고(AD 렌더 해상도 500x500), 머리가 독립 회전 못 함.
- 큰 동작(팔 흔들기 등)에서는 Plan A가 훨씬 안정적으로 확인됨 (Plan B는 몸통이 크게 움직이면 머리 추적이 깨짐).

---

## 3. 캐릭터 디자인 10원칙 (이 파이프라인에 최적화)

1. 목이 아예 없는 머리/몸통 분리 원본
2. 팔다리가 몸통과 절대 겹치지 않는 A-포즈
3. 그림자·그라데이션 없는 완전 납작한 단색 (특히 **피부**는 무조건 flat — 음영/해칭 있으면 색칠·변형이 힘들어짐)
4. 굵고 끊김 없이 완전히 닫힌 윤곽선 (선에 작은 틈만 있어도 색이 샘)
5. 머리카락은 한 올 한 올이 아니라 뭉텅이 덩어리로 (질감/해칭은 머리카락·옷감에는 OK, 피부에는 금지)
6. 머리는 (거의) 원형 실루엣
7. 눈/입은 머리카락에 안 가려지게, 위치 고정
8. 목/어깨 경계를 옷깃(터틀넥 등)으로 가리는 디자인
9. 옷은 무늬 없이 플레인하게, 패턴은 나중에 코드로
10. 신체 부위별로 색 영역이 하나씩만 있게

---

## 4. 코드로 처리 가능한 것들 (전부 검증됨)

- **색칠**: OpenCV flood-fill. 단, 실루엣 마스크(ink+dilate+corner flood-fill 방식)를 미리 만들어서 "벽"으로 써야 함 — 색상 기반 flood-fill만 쓰면 그림 틈으로 새어나감.
- **패턴 추가**: 셔츠에 대각선 스트라이프 등, 좌표 기반으로 영역 마스킹 후 합성.
- **텍스처/질감**: 얼굴은 피하고 옷/머리에만.
- **숏폼/롱폼 포맷팅**: 9:16, 16:9 등 표준 비율로 letterbox/pillarbox 처리해서 한 소스에서 여러 규격 출력.

## 5. 자주 겪은 버그와 원인

| 증상 | 원인 |
|---|---|
| 어깨에 이중선(고스트) | 머리 원형 크롭이 몸통 옷깃 영역까지 침범해서 겹침 |
| 목이 가슴팍까지 내려옴 | 머리 붙이는 좌표 계산 실수 (중심점을 잘못 정렬) |
| 입이 겹쳐 보임 | 원본 그림에 이미 그려진 입을 안 지우고 그 위에 새 입을 얹음 |
| 귀가 잘림 | 원형 크롭 반지름이 실제 그림 크기보다 작았음 |
| 머리 주변 유령 테두리 | 크롭 범위가 원본 이미지 경계를 벗어나서 PIL이 투명 검정으로 패딩 → 페더링 마스크가 그걸 비치게 함 |
| 피부색 칠할 때 배경까지 물듦 | 색상 기반 flood-fill이 선의 작은 틈으로 새어나감 → 실루엣 마스크를 벽으로 사용해서 해결 |

---

## 6. 지금까지 만든 캐릭터

- **anime 소년** (세션 최초, lipsync 테스트용)
- **손그림 소년** (사용자 원본 스케치 → A-포즈로 재생성한 `apose` 캐릭터, 현재 메인 테스트 캐릭터)
- **10원칙 검증용 남/여 캐릭터**
- **병맛 캐릭터** (대머리, 이목구비 중앙에 몰림, 감자몸통) — 색칠/애니메이션까지 완성
- **너드 캐릭터** (남/여, 큰 뿔테 안경+테이프, 뻐드렁니, 포켓 프로텍터) — 진행 중
- **하얀 안경알 캐릭터** (참고 이미지 기반, 회색 후드) — 진행 중

## 7. 남은 논의/미해결

- "일부러 못 그린 느낌" 스타일: AI 생성 모델 자체가 "잘 그리려는" 편향이 강해서 프롬프트만으로 조절이 어려움 → 코드로 선을 흔드는(wobbly point) 방식이 더 확실할 수 있음
- 롱폼 프레이밍 시 배경(양옆 여백)을 어떻게 채울지 — 배경 이미지 준비 필요
- 실제로 대부분의 참고 채널이 상반신 위주로 화면을 잡는다는 관찰 → 하체 리깅 투자 우선순위 낮출지 결정 필요

---

*작업 파일들은 스크래치패드(`/tmp/.../scratchpad/`)에 있고, 리포에는 핵심 스크립트만 커밋되어 있음. 이 문서는 대화 요약이며 git에는 커밋하지 않았음 (필요하면 말씀해주세요).*
